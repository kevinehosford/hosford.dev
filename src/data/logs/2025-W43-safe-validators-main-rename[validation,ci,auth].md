### Overview
Two big ones this week: every route validator stopped throwing on unexpected search params, and the `nexus` → `main` rename finally landed. Around that, fixed IDP-initiated sessions so they carry an IP and user agent, narrowed the trace/ingest rewrites so we don't accidentally match datasets named "traces", and stopped the heatmap from blanking the smallest bucket.

### Highlights
- [#5907](https://github.com/axiomhq/app/pull/5907) — Swapped `zodValidator` for `safeZodValidator` across every `validateSearch` in the route tree and every `inputValidator` on the server functions, so a stray param no longer takes down stream/dashboards/monitors/flows/etc. The new validator logs the parse error and returns a usable default instead of throwing.
- [#5926](https://github.com/axiomhq/app/pull/5926), [#5902](https://github.com/axiomhq/app/pull/5902) — Renamed `nexus` → `main` (and shifted old main to `main-legacy`), then chased the rename through every workflow: dropped the old dev/prod Docker build and k8s deploy jobs, retitled the deploy preview/production/staging flows, fixed the coverage report paths, and removed `release-staging.yml` and `seed-e2e-playwright.yml`. Also moved CI off `depot-ubuntu-22.04-16` onto `ubuntu-large-runner`.
- [#5910](https://github.com/axiomhq/app/pull/5910) — Session management is fully on better-auth now: added a `DeviceSession` model, a `getDeviceSessionsQuery()`, and optimistic delete / delete-all-others mutations on top of React Query. IDP-initiated OAuth now forwards request headers so the resulting session actually records the IP and user agent instead of leaving the row half-empty.
- [#5928](https://github.com/axiomhq/app/pull/5928), [#5925](https://github.com/axiomhq/app/pull/5925) — Tightened the nitro rewrite rules to `^/api/v1/datasets/([^/]+)/ingest$` and `^/api/v1/traces$` exactly so datasets with "traces" in the name stopped getting hijacked, and moved Sentry/Faro/image-proxy/Netlify-integration endpoints off `/api/exapi/*` onto `/exapi/*` to unstick the Netlify redirect URI.
- [#5913](https://github.com/axiomhq/app/pull/5913), [#5933](https://github.com/axiomhq/app/pull/5933) — Overhauled the Playwright project layout: split into setup/teardown projects for notifiers/monitors/endpoints/http-logs, added a `globalSetup` with a shared run ID, made seeds deterministic with `startTime`/`endTime`/`numEvents`, and re-enabled a pile of dashboard/monitor tests. Separately, fixed `makeColorScales` so heatmaps with `max === min` or a tiny range still produce two buckets with real colors.
